<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d"
	x:Class="Ccom.Pharmacy.Views.GeneralDataHome"
	d:DesignWidth="1366" d:DesignHeight="700">
    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Padding="5" Background="{DynamicResource AccentColorBrush4}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="StkButtonList"/>
            </ScrollViewer>
        </Border>
        <Grid x:Name="GrdMainView" Grid.Column="1" Margin="5,0,0,0"/>
    </Grid>
</UserControl>